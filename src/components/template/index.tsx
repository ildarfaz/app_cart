import { FC } from 'react';

import styles from './style.module.scss';

type TTemplate = {

};

export const Template: FC<TTemplate> = () => {

    return (
        <div className={styles.Template}>
            Template
        </div>
    );
}
